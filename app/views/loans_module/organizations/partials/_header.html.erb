<%= link_to organization_path(organization) do %>
  <div class="card">
    <div class="card-body">
      <%= organization.name %>
      <%= organization.member_loans.count %>
    </div>
  </div>
<% end %>
