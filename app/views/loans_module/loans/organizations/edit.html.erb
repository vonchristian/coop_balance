<%= simple_form_for @loan, url: :loans_module_loan_organization do |f| %>
  <%= f.input :organization_id, collection: Organization.all.map{|a| [a.name, a.id] } %>
  <%= f.submit "Save Organization", class: "btn btn-danger" %>
<% end %>
