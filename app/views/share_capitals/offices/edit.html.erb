<%= simple_form_for @share_capital, url: :share_capital_office do |f| %>
  <%= f.input :office_id, collection: current_user.cooperative.offices.map{|a| [ a.name.titleize, a.id] }, as: :radio_buttons %>
  <%= f.submit "Update Branch Office" %>
<% end %>
