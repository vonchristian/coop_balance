<div class="row">
  <div class="col-md-8 offset-md-2">
    <div class="card">
      <div class="card-header bg-light">
        <div class="row">
          <div class="col-md-8">
            <h3 class="card-title"> Transaction Details </h3>
          </div>
          <div class="col-md-4">
            <%=link_to 'View (PDF)', voucher_path(@voucher, format: "pdf"), class: "btn btn-default float-right" %>
          </div>
        </div>
      </div>
        <div class="card-body">

        <%= render 'vouchers/partials/alert_message', voucher: @voucher %>

      <%= render 'vouchers/partials/body', voucher: @voucher %>
      <% if !@voucher.disbursed?%>
          <%= button_to "Confirm", program_subscription_voucher_confirmations_path(voucher_id: @voucher.id, program_subscription_id: @program_subscription.id), method: :post, class: "btn btn-primary float-right", data: { disable_with: "Processing..." } %>
          <%= button_to "Cancel", program_subscription_voucher_path(id: @voucher.id, program_subscription_id: @program_subscription.id), method: :delete, class: "btn text-danger float-left" %>
        </div>
      <% else %>
        <%= render 'vouchers/partials/status', voucher: @voucher %>
      <% end %>
    </div>
  </div>
</div>
