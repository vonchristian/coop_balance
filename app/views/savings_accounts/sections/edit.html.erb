<%= simple_form_for @savings_account, url: :savings_account_section do |f| %>
  <%= f.input :section_id, collection: current_user.branch_office.sections.all.map{|a| [a.name, a.id] }, as: :radio_buttons %>
  <%= f.submit "Update" %>
<% end %>
