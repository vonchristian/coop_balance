<div class="row">
  <div class="col-md-8 offset-md-2">
    <div class="card">
      <div class="card-header bg-light">
        <div class="row">
          <div class="col-md-6">
            <h3 class="card-title"> Voucher Details </h3>
          </div>
          <div class="col-md-6">
            <%=link_to 'View (PDF)', voucher_path(@voucher, format: "pdf"), class: "btn btn-default float-right" %>
          </div>
        </div>
      </div>
      <div class="card-body no-padding-bottom">
         <p class="alert alert-warning no-margin-bottom"><i class="fa fa-info-circle fa-lg"></i> Please review the following details below. If no errors are found, Click "Disburse" to post entry.</p>
      </div>
      <%= render 'vouchers/partials/body', voucher: @voucher %>
      <% if !@voucher.disbursed?%>
        <hr class="no-margin">
        <div class="card-footer">
          <%= link_to "Cancel", voucher_path(@voucher), method: :delete, class: "btn text-danger float-left" %>
          <%= link_to "Disburse", loan_application_voucher_confirmations_path(loan_application_id: @loan_application.id, voucher_id: @voucher.id), method: :post, class: "btn btn-primary float-right", data: { disable_with: "Processing..." } %>
        </div>
      <% else %>
        <%= render 'vouchers/partials/status' %>
      <% end %>
    </div>
  </div>
</div>
