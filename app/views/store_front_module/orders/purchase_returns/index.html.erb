<div class="row">
  <div class="col-md-12">
    <div class="box box-danger with-border">
      <div class="box-header">
        <div class="row">
          <div class="col-md-4">
            <h3 class="box-title"> Purchase Returns </h3>
          </div>
          <div class="col-md-5">
          </div>
          <div class="col-md-3">
            <span class="pull-right">
              <%= link_to "New Purchase Return", new_store_front_module_purchase_return_line_item_path, class: "btn btn-danger" %>
            </span>
          </div>
        </div>
      </div>
      <div class="box-body">
        <div class="table table-responsive">
          <table class="table table-striped table-hover">
            <thead>
              <tr>
                <th> Date </th>
                <th> Supplier </th>
                <th> Items </th>
                <th> Employee </th>
              </tr>
            </thead>
            <tbody>
              <% @purchase_return_orders.each do |purchase_return_order| %>
                <tr>
                  <td><%= purchase_return_order.date.strftime("%B %e, %Y") %></td>
                  <td><%= purchase_return_order.supplier_name %></td>
                  <td>
                    <% purchase_return_order.purchase_return_line_items.each do |line_item| %>
                      <div class="row">
                        <div class="col-md-3">
                          <%= line_item.quantity %> <%= line_item.unit_of_measurement_code %>
                        </div>
                        <div class="col-md-6">
                          <%= line_item.name %>
                        </div>
                        <div class="col-md-3">
                          <%= number_to_currency line_item.total_cost %>
                        </div>
                      </div>
                      <br>
                    <% end %>
                  </td>
                  <td><%= purchase_return_order.employee_name %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

