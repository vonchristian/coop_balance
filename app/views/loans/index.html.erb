<div class="row">
  <div class="col-md-12">
    <div class="box box-solid">
      <div class="box-header">
      </div>
      <div class="box-body">
    <div class="nav-tabs-navigation">
      <ul class="nav nav-tabs">
        <li class="<%= 'active' if current_page?(loans_path)%>"><%= link_to 'Info', loans_path %></li>
        <li class="<%= 'active' if current_page?(filtered_loans_path)%>"><%= link_to 'Filters', filtered_loans_path %></li>
        <li class="<%= 'active' if current_page?(loans_path)%>"><%= link_to 'Settings', loans_path %></li>
    </ul>
    <div class="tab-content">
      <% if current_page? filtered_loans_path %>
        <%= render 'loans/partials/filtered_loans' %>
      <% end %>
      <% if current_page? loans_path %>
        <%= render 'loans/partials/all_loans' %>
      <% end %>

