<div class="modal-body">
  <center>
    <%= image_tag(@loan.borrower.avatar, width: 60, height: 60, class: 'media-object img-circle') if @loan.borrower.avatar.attached? %>
    <h4 class="card-title"><%= @loan.borrower_name %></h4>
  </center>
</div>
<hr class="no-margin">