<div class="row">
  <div class="col-md-12">
    <div class="nav-tabs-custom">
      <div class="box-header">
        
           <h4><center><%= member_avatar(@savings_account.member, style: 'width:60px;height:60px;', class: 'media-object img-circle') %></center></h4>
           
            <h3><center><%= @savings_account.member.first_and_last_name %></center></h3>
      </div>
     <div class="box-body">
        <div class="row">
          <div class="col-md-3 border-right">
            <small class="text-muted">DEPOSITS </small>
            <h4><%= number_to_currency @savings_account.deposits %></h4>
            <%= link_to 'New Deposit', new_teller_department_savings_account_deposit_path(@savings_account), class: "btn btn-primary btn-sm" %>
          </div>
          <div class="col-md-3 border-right">
            <small class="text-muted">WITHDRAWALS </small>
            <h4><%= number_to_currency @savings_account.withdrawals %></h4>
            <%= link_to 'New Withdraw', new_teller_department_savings_account_withdrawal_path(@savings_account), class: "btn btn-danger btn-sm" %>
          </div>
          <div class="col-md-3 border-right">
            <small class="text-muted">EARNED INTERESTS</small>
            <h4><%= number_to_currency @savings_account.interests_earned %></h4>
          </div>
          <div class="col-md-3 border-right">
            <small class="text-muted">BALANCE </small>
            <h4><%= number_to_currency @savings_account.balance %></h4>
          </div>
        </div>
         <hr>
         <p>TRANSACTIONS </p>
         <div class="table table-responsive">
            <table class="table table-hover table-striped">
              <thead>
                <tr>
                  <th> Date </th>
                  <th> Amount </th>
                  <th> Type </th>
                  <th> Employee </th>
                </tr>
              </thead>
              <tbody>
                <% @savings_account.entries.each do |entry| %>
                  <tr>
                    <td><%= entry.entry_date.strftime("%B %e, %Y") %></td>
                    <td><%= number_to_currency entry.credit_amounts.sum(:amount) %></td>
                    <td><%= entry.entry_type %></td>
                    <td><% if entry.recorder %>
                      <%=image_tag(entry.recorder.avatar.url(:small), class: "img-circle", height: 35, width: 35) %> &nbsp;
                      <%= entry.recorder_first_and_last_name %>
                      <% end %>
                     </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
