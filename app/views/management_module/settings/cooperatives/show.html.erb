<%= link_to 'Add Office', new_management_module_settings_cooperative_office_path(@cooperative) %>

<% @offices.each do |office| %>
  <%= link_to office.name, office_path(office) %>
<% end %>
<center><%= will_paginate @offices %></center>
