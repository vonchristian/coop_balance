<div class="row">
  <div class="col-md-6 offset-md-3">
    <div class="card">
      <div class="card-header">
        <center>
          <%= image_tag("bank", width: 45, height: 45, class: "img-circle") %>
          <h3 class="card-title"><%= @bank_account.bank_name %></h3>
          <small><%= @bank_account.bank_address %></small><br>
          <small><%= @bank_account.account_number %></small>
          <hr class="no-margin">
          <small class="text-muted">ACCOUNT BALANCE</small>
          <h3 class="card-title"><%= number_to_currency @bank_account.balance %></h3>
        </center>
      </div>
      <div class="card-body">
        <h3 class="card-title"> Widthraw Details </h3>
        <hr class="no-margin-top">
        <%= simple_form_for [@bank_account, @entry], url: :bank_account_withdrawals, html: { auticomplete: "off" } do |f| %>
          <%= f.error_notification %>
          <%= f.input :date, as: :string, input_html: { class: "datepicker" } %>
          <%= f.input :amount, as: :numeric %>
          <%= f.input :reference_number %>
          <%= f.input :description %>
          <%= f.input :bank_account_id, input_html: { value: @bank_account.id },as: :hidden %>
          <%= f.input :recorder_id, input_html: { value: current_user.id },as: :hidden %>
          <%= f.input :credit_account_id, input_html: { value: @bank_account.account_id }, as: :hidden %>
          <%= f.input :debit_account_id, collection: AccountingModule::Account.all.map{|a| [a.name, a.id]} %>


          <%= f.submit "Save Withdraw", class: "btn btn-danger float-right", data: { disable_with: "Processing..." } %>

        <% end %>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript">
  $(document).ready(function() {
    $("#vouchers_voucher_amount_processing_account_id").select2({
      theme: "bootstrap"
    });
    $("#vouchers_voucher_amount_processing_amount_type").select2({
      theme: "bootstrap"
    });
     $("#bank_accounts_entry_form_debit_account_id").select2({
      theme: "bootstrap"
    });
  });
</script>

